** Overview

This file contains all configuration variables used throughout the installation.

*IMPORTANT*: Customize these values before tangling!

All other phases will reference these variables.

** Ansible Inventory

#+begin_src yaml :tangle ../ansible/inventory.yml
---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
#+end_src

** Ansible Configuration

#+begin_src ini :tangle ../ansible/ansible.cfg
[defaults]
inventory = inventory.yml
host_key_checking = False
retry_files_enabled = False
gathering = smart
fact_caching = jsonfile
fact_caching_connection = /tmp/ansible_facts
fact_caching_timeout = 86400
stdout_callback = default
interpreter_python = auto_silent
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
#+end_src

** Global Variables

#+begin_src yaml :tangle ../ansible/group_vars/all.yml
---
# ============================================================================
# Global Configuration Variables (System Defaults)
# ============================================================================
# Generated from org/00-configuration.org
#
# User-editable values are in config.yml (project root)
# These are system defaults that rarely need changing

# System version
system_version: "{{ lookup('file', '../VERSION') | trim }}"

# ----------------------------------------------------------------------------
# Network Ports
# ----------------------------------------------------------------------------

# Tor SOCKS proxy port
tor_socks_port: 9050

# Tor control port
tor_control_port: 9051

# DNS port for Tor
tor_dns_port: 5353

# Monero daemon installation
monerod_install_dir: /opt/monero
# Monero ZMQ pub port (required by P2Pool)
monerod_zmq_port: 18083

# P2Pool port mapping
p2pool_port_map:
  normal: 37889
  mini: 37888
  nano: 37890

# P2Pool P2P port (derived from type)
p2pool_p2p_port: "{{ p2pool_port_map[p2pool_type] }}"

# P2Pool host and stratum port
p2pool_host: "127.0.0.1"
p2pool_stratum_port: 3333

# Wallet RPC

monero_wallet_rpc_port: 18084
monero_wallet_rpc_restricted: true

# ----------------------------------------------------------------------------
# WireGuard Configuration
# ----------------------------------------------------------------------------

wireguard_network: 10.8.0.0/24
wireguard_server_ip: 10.8.0.1
wireguard_port: 51820
wireguard_only: false

# ----------------------------------------------------------------------------
# Firewall Rules
# ----------------------------------------------------------------------------

# Service-specific firewall rules
# SSH rules handled separately based on wireguard_only flag
firewall_service_rules:
  - { port: "{{ wireguard_port }}", proto: "udp", comment: "WireGuard VPN" }
  - { port: "{{ p2pool_p2p_port }}", proto: "tcp", comment: "P2Pool P2P" }
  - { port: "{{ p2pool_p2p_port }}", proto: "udp", comment: "P2Pool P2P" }
  - { port: "18080", proto: "tcp", comment: "Monero P2P" }

# ----------------------------------------------------------------------------
# Storage Configuration
# ----------------------------------------------------------------------------

# Mining data directory
mining_data_dir: /opt/mining

# P2Pool installation directory (base path - version in config.yml)
p2pool_install_dir: /opt/p2pool

# P2Pool data directory (persistent state, separate from install)
p2pool_data_dir: /var/lib/p2pool

# XMRig installation directory (base path - version in config.yml)
xmrig_install_dir: /opt/xmrig

# Haveno (RetoSwap) exchange — desktop GUI app, off by default (haveno_enabled
# in config.yml). Installed as the signed AppImage; run on demand under
# bubblewrap, never as a service.
haveno_enabled: false
haveno_install_dir: /opt/haveno
# Default haveno-reto data dir on Linux. Holds the trade wallet + Tor onion
# key, so it sits OUTSIDE ~/.secrets (the sandbox blanks .secrets, but Haveno
# needs the wallet at runtime). Backed up by the backup phase.
haveno_data_dir: "/home/{{ dev_user }}/.local/share/Haveno-reto"
# The release tag is e.g. "1.6.0-reto"; the published asset embeds the upstream
# Haveno version ("v1.6.0"). Derive the asset name from the tag so a version
# bump only touches haveno_version in config.yml.
haveno_appimage_version: "v{{ haveno_version | regex_replace('-reto$', '') }}"
haveno_appimage_name: "haveno-{{ haveno_appimage_version }}-linux-x86_64.AppImage"

# Interop: cast (Foundry) EVM-chain tooling — CLI signer, off by default
# (cast_enabled in config.yml). The first tier beyond the Monero-native core.
# Verified via cosign/Sigstore (Foundry publishes no GPG key); run on demand
# inside a fail-closed Tor network namespace, never as a service.
cast_enabled: false
foundry_install_dir: /opt/foundry
# Release assets derived from the pinned tag (config.yml: foundry_version). Pin
# a VERSIONED tag (vX.Y.Z) — the rolling `stable` tag omits the .sha256 +
# .sigstore.json we verify against.
foundry_release_base: "https://github.com/foundry-rs/foundry/releases/download/{{ foundry_version }}"
foundry_tarball: "foundry_{{ foundry_version }}_linux_amd64.tar.gz"
foundry_sha_file: "foundry_{{ foundry_version }}_linux_amd64.sha256"
foundry_sigstore_file: "foundry_{{ foundry_version }}_linux_amd64.sigstore.json"
# Pinned cosign certificate identity: the Foundry release workflow signs the
# archive keyless via GitHub Actions OIDC. The identity is the workflow path at
# THIS exact tag, so a binary signed by any other workflow/tag/repo fails closed.
# See keys/README.md for provenance. (This is the Sigstore analog of a pinned
# GPG key + signed-hashes chain.)
foundry_cosign_identity: "https://github.com/foundry-rs/foundry/.github/workflows/release.yml@refs/tags/{{ foundry_version }}"
foundry_cosign_issuer: "https://token.actions.githubusercontent.com"
# Fail-closed Tor network namespace for cast (set up on demand by cast-tor).
# A dedicated Tor runs inside it; nftables redirects cast's traffic to Tor and
# drops everything else. Independent of the system Tor (privacy phase).
interop_netns: interop
interop_veth_host_ip: "10.200.0.1"
interop_veth_ns_ip: "10.200.0.2"
interop_veth_subnet: "10.200.0.0/24"
interop_tor_trans_port: 9040
interop_tor_dns_port: 9053
interop_tor_data_dir: /var/lib/tor-interop
# Encrypted cast keystore (no-HW-wallet fallback only; HW signing leaves no key
# on disk). Default `cast wallet import` location. Backed up if present.
foundry_keystore_dir: "/home/{{ dev_user }}/.foundry/keystores"

# MSR (model-specific register) access for XMRig. Gives ~5-15% more
# hashrate but grants the miner cap_sys_rawio + kernel MSR write access
# — real hardware-level privilege for a network-facing workload.
# Default off; opt in via config.yml.
mining_msr_access: false

# Backup directory
backup_dir: /srv/backups

# Backup encryption (age) — recipient encrypts, identity decrypts.
# The identity lives OUTSIDE backup_dir and is never included in backups;
# keep an offline copy (KeePassXC / paper) or backups are unrecoverable
# after disk loss.
backup_age_identity: /etc/backup/age-identity.txt
backup_age_recipient: /etc/backup/age-recipient.txt

# Dedicated root-only GPG keyring for verifying release downloads.
# Populated from the pinned maintainer keys committed in keys/ —
# see keys/README.md for fingerprints and provenance.
gpg_verify_home: /var/lib/gpg-verify

# VM storage location
libvirt_images_path: /var/lib/libvirt/images

# ----------------------------------------------------------------------------
# Resource Limits
# ----------------------------------------------------------------------------

# Memory limits (MB per user)
memory_limit_mining: 4096

# ----------------------------------------------------------------------------
# System Configuration
# ----------------------------------------------------------------------------

# Kernel parameters for mining optimization
# Both monerod (fast verification) and XMRig allocate a full RandomX
# dataset (~1168 pages each); 1280 only fits one of them.
kernel_params:
  - { name: "vm.nr_hugepages", value: "2560" }  # For huge pages

# Swap configuration
swappiness: 10  # Low swappiness for server workload
#+end_src

** Configuration Notes

*** Monero Wallet Address

Get a Monero wallet address from:
- Official Monero GUI/CLI wallet
- MyMonero (web wallet)
- Cake Wallet (mobile)
- Monerujo (mobile)

The address should start with =4= (mainnet) and be 95 characters long.

*** Mining Threads

- =0= - Use all CPU cores
- =N= - Use N specific cores
- ={{ ansible_processor_vcpus - 2 }}= - Leave 2 cores for other work

*** P2Pool Mini vs Main

- *P2Pool Mini*: For hashrates < 100 kH/s (most home systems)
- *P2Pool Main*: For hashrates > 100 kH/s (large miners)

Use mini pool unless you have high-end hardware.

*** P2Pool Version

Check https://github.com/SChernykh/p2pool/releases for latest version.

Set to ="latest"= to always download the most recent release, or pin to specific version like ="v4.1"= for stability.

*** Firewall Ports

Only open ports you actually need. Default is SSH only.

Add application ports as needed (80, 443, etc.)

P2Pool P2P port (37889) will be automatically opened during mining setup.

*** Resource Limits

Adjust based on your hardware. Current settings assume:
- 8+ CPU cores
- 16+ GB RAM
- Privacy-focused work runs concurrently with mining

Mining service has dedicated resource limits to prevent
interference with other system operations.
